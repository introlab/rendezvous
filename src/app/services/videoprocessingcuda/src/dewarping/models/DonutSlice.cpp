#include "DonutSlice.h"

DonutSlice::DonutSlice(float xCenter, float yCenter, float inRadius, float outRadius, float middleAngle, float angleSpan)
    : xCenter(xCenter),
    yCenter(yCenter),
    inRadius(inRadius),
    outRadius(outRadius),
    middleAngle(middleAngle),
    angleSpan(angleSpan)
{
}